import { useCallback,useEffect,useState } from "react"; import { getCertificate } from "../services/certificate/getCertificates"; export default function useCertificates(tokenId){const [data,setData]=useState(null),[loading,setLoading]=useState(!!tokenId),[error,setError]=useState(""); const load=useCallback(async()=>{if(!tokenId)return;setLoading(true);setError("");try{setData(await getCertificate(tokenId))}catch(e){setError(e.message)}finally{setLoading(false)}},[tokenId]);useEffect(()=>{load()},[load]);return {data,loading,error,refresh:load}}
