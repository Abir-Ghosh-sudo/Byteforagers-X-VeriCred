import VerifiedBadge from "./VerifiedBadge"; import { formatAddress } from "../../utils/formatAddress"; import { formatDate } from "../../utils/formatDate"; export default function VerificationResult({result}){if(!result)return null;const valid=result.valid;return <div className="card verification-result"><div className="row-between"><div><h2>{valid?"Certificate is authentic":"Certificate is not valid"}</h2><p>Blockchain verification result</p></div><VerifiedBadge valid={valid}/></div><div className="details-grid"><div><span>Recipient</span><strong>{formatAddress(result.recipient,6)}</strong></div><div><span>Issuer</span><strong>{formatAddress(result.issuer,6)}</strong></div><div><span>Issued</span><strong>{formatDate(result.issuedAt)}</strong></div><div><span>Status</span><strong>{result.revoked?"Revoked":"Active"}</strong></div></div></div>}
