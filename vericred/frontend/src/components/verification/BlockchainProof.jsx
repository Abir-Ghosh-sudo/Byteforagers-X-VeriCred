import { ExternalLink } from "lucide-react"; import { env } from "../../config/environment"; import { contractAddress } from "../../contracts/contractConfig"; export default function BlockchainProof({tokenId}){return <div className="card proof"><h3>Blockchain proof</h3><div className="proof-line"><span>Network</span><strong>Sepolia</strong></div><div className="proof-line"><span>Contract</span><strong className="mono">{contractAddress||"Not configured"}</strong></div>{tokenId&&<a href={`https://sepolia.etherscan.io/token/${contractAddress}?a=${tokenId}`} target="_blank" rel="noreferrer">View on Etherscan <ExternalLink size={15}/></a>}</div>}
