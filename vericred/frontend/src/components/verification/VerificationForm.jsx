import { useState } from "react"; import Button from "../common/Button"; export default function VerificationForm({onVerify,loading,initialValue=""}){const [value,setValue]=useState(initialValue);return <form className="verify-form" onSubmit={e=>{e.preventDefault();if(value.trim())onVerify(value.trim())}}><input value={value} onChange={e=>setValue(e.target.value)} placeholder="Enter certificate Token ID" inputMode="numeric"/><Button type="submit" loading={loading}>Verify certificate</Button></form>}
