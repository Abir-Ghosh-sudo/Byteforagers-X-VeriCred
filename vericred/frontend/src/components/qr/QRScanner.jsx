import { useState } from "react"; import { useNavigate } from "react-router-dom"; import Button from "../common/Button"; export default function QRScanner(){const [value,setValue]=useState("");const nav=useNavigate();const scan=()=>{const m=value.match(/(?:verify\/|tokenId=)([0-9]+)/i);const id=m?.[1]||value.trim();if(id)nav(`/verify/${id}`)};return <div className="card"><h3>Scan / paste QR result</h3><p className="muted">For the demo, paste the URL or token ID read from a QR scanner.</p><div className="verify-form"><input value={value} onChange={e=>setValue(e.target.value)} placeholder="https://.../verify/12 or 12"/><Button onClick={scan}>Open</Button></div></div>}
