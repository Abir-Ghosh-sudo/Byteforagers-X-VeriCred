import { Link,NavLink } from "react-router-dom"; import { ShieldCheck,Menu,X } from "lucide-react"; import { useState } from "react"; import ConnectWallet from "../wallet/ConnectWallet"; export default function Navbar(){const [open,setOpen]=useState(false);return <header className="navbar"><Link className="brand" to="/"><ShieldCheck size={27}/><span>VeriCred</span></Link><button className="mobile-menu" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><nav className={open?"nav-links open":"nav-links"}>{[["/","Home"],["/verify","Verify"],["/dashboard","Issuer"],["/admin","Admin"]].map(([to,l])=><NavLink key={to} to={to} end={to==="/"} onClick={()=>setOpen(false)}>{l}</NavLink>)}<ConnectWallet/></nav></header>}
