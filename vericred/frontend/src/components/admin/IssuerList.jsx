import { formatAddress } from "../../utils/formatAddress"; export default function IssuerList({issuers=[]}){return <div className="card"><div className="section-title"><h3>Authorized issuers</h3><span>{issuers.length}</span></div>{issuers.length?<div className="table">{issuers.map((x,i)=><div className="table-row" key={x.address||i}><span>{x.name||`Issuer ${i+1}`}</span><span className="mono">{formatAddress(x.address,6)}</span></div>)}</div>:<p className="muted">No issuer records returned by the API.</p>}</div>}
