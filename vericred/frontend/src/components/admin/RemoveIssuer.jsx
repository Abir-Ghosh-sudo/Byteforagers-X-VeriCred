import { useState } from "react"; import Button from "../common/Button"; import { isAddress } from "../../utils/validation"; export default function RemoveIssuer({onRemove,loading}){const [address,setAddress]=useState("");return <div className="card"><h3>Remove issuer</h3><div className="inline-form"><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="0x issuer wallet"/><Button variant="danger" loading={loading} disabled={!isAddress(address)} onClick={()=>onRemove(address)}>Remove</Button></div></div>}
