import { useState } from "react"; import Button from "../common/Button"; import { isAddress } from "../../utils/validation"; export default function AddIssuer({onAdd,loading}){const [address,setAddress]=useState("");return <div className="card"><h3>Add authorized issuer</h3><p className="muted">Only the contract owner can complete this transaction.</p><div className="inline-form"><input value={address} onChange={e=>setAddress(e.target.value)} placeholder="0x issuer wallet"/><Button loading={loading} disabled={!isAddress(address)} onClick={()=>onAdd(address)}>Add issuer</Button></div></div>}
