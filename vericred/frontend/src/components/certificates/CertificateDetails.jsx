import { formatAddress } from "../../utils/formatAddress"; import { formatDate } from "../../utils/formatDate"; export default function CertificateDetails({certificate}){if(!certificate)return null;return <div className="card"><div className="section-title"><h2>Certificate details</h2><span className={`status ${certificate.revoked?"revoked":"valid"}`}>{certificate.revoked?"Revoked":"Valid"}</span></div><div className="details-grid"><div><span>Token ID</span><strong>#{certificate.tokenId||certificate.token_id}</strong></div><div><span>Recipient</span><strong>{formatAddress(certificate.recipient,6)}</strong></div><div><span>Issuer</span><strong>{formatAddress(certificate.issuer,6)}</strong></div><div><span>Issued</span><strong>{formatDate(certificate.issuedAt||certificate.issued_at)}</strong></div><div className="wide"><span>Metadata CID</span><strong className="mono">{certificate.metadataCID||certificate.metadata_cid||"—"}</strong></div></div></div>}
