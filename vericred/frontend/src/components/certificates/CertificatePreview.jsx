import { formatDate } from "../../utils/formatDate"; export default function CertificatePreview({certificate}){return <div className="certificate-preview"><div className="cert-border"><span className="eyebrow">VERICRED • BLOCKCHAIN CREDENTIAL</span><h1>Certificate of Achievement</h1><p>This certificate is proudly presented to</p><h2>{certificate?.name||"Student Name"}</h2><p>for successfully completing</p><h3>{certificate?.course||"Course / Credential"}</h3><p>{certificate?.institution||"Institution"}</p><div className="preview-bottom"><span>Token #{certificate?.tokenId||"—"}</span><span>{formatDate(certificate?.issuedAt||certificate?.date)}</span></div></div></div>}
