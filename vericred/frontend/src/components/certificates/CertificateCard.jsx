import { Link } from "react-router-dom"; import { Award,ExternalLink } from "lucide-react"; import { formatDate } from "../../utils/formatDate"; export default function CertificateCard({certificate}){return <div className="card cert-card"><div className="cert-icon"><Award/></div><div className="cert-main"><div className="row-between"><div><h3>{certificate.name||certificate.student_name||"Certificate"}</h3><p>{certificate.course||certificate.title||"Credential"}</p></div><span className={`status ${certificate.revoked?"revoked":"valid"}`}>{certificate.revoked?"Revoked":"Valid"}</span></div><div className="cert-meta"><span>Token #{certificate.tokenId||certificate.token_id}</span><span>{formatDate(certificate.issuedAt||certificate.issued_at)}</span></div></div><Link className="icon-btn" to={`/certificate/${certificate.tokenId||certificate.token_id}`}><ExternalLink/></Link></div>}
