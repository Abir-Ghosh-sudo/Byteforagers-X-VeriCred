import CertificateCard from "../certificates/CertificateCard"; export default function IssuedCertificates({certificates=[]}){return <section><div className="section-title"><h2>Issued certificates</h2><span>{certificates.length}</span></div>{certificates.length?<div className="stack">{certificates.map((c,i)=><CertificateCard certificate={c} key={c.tokenId||i}/>)}</div>:<div className="empty card">No certificates yet. Issue your first credential.</div>}</section>}
