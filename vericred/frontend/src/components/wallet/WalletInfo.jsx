import { formatAddress } from "../../utils/formatAddress"; import useWallet from "../../hooks/useWallet"; export default function WalletInfo(){const {account,chainId,network}=useWallet();return <div className="wallet-info"><div><span>Wallet</span><strong>{formatAddress(account,6)}</strong></div><div><span>Network</span><strong>{network?.name||chainId||"Not connected"}</strong></div></div>}
