import { Wallet } from "lucide-react"; import Button from "../common/Button"; import useWallet from "../../hooks/useWallet"; import { formatAddress } from "../../utils/formatAddress"; export default function ConnectWallet(){const {account,connect,disconnect,loading}=useWallet();if(account)return <button className="wallet-pill" onClick={disconnect}><span className="dot"/> {formatAddress(account)}</button>;return <Button onClick={()=>connect().catch(e=>alert(e.message))} loading={loading}><Wallet size={17}/> Connect Wallet</Button>}
