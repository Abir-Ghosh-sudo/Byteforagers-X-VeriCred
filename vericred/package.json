{
  "name": "vericred",
  "version": "1.0.0",
  "description": "On-chain verifiable credentials and soulbound certificates",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network sepolia",
    "setup-issuer": "hardhat run scripts/setupIssuer.js --network sepolia",
    "verify": "hardhat run scripts/verifyContract.js --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.18"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
